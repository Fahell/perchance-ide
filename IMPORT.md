# Import URL (Branch: feat/terminal-and-shell-tools)

Add an HTML panel to your generator with:

```html
<script>
  import("https://cdn.jsdelivr.net/gh/Fahell/perchance-ide@e11c04cbd49e41abb368bc7707b91f914ce1c582/dist/agent.js");
</script>
```

Also add `agentAi = {import:ai-text-plugin}` to your list panel.

> 📌 This URL points to commit `e11c04cbd49e41abb368bc7707b91f914ce1c582` on branch `feat/terminal-and-shell-tools`.
> Each push generates a new unique URL (cache-busting). For production, use `pnpm deploy` on `main`.
