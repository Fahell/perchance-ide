# Import URL (Branch: feat/terminal-and-shell-tools)

Add an HTML panel to your generator with:

```html
<script>
  import("https://cdn.jsdelivr.net/gh/Fahell/perchance-ide@feat/terminal-and-shell-tools/dist/agent.js");
</script>
```

Also add `agentAi = {import:ai-text-plugin}` to your list panel.

> ⚠️ This URL points to the **latest commit** on branch `feat/terminal-and-shell-tools`.
> It auto-updates on every push. For production, use `pnpm deploy` on `main`.
