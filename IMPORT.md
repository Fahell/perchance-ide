# Import URL

Add an HTML panel to your generator with:

```html
<script>
  import("https://cdn.jsdelivr.net/gh/Fahell/perchance-ide@061a155/dist/agent.js");
</script>
```

Also add `agentAi = {import:ai-text-plugin}` to your list panel.
